@extends('layouts.app') @section('content')<div class="page-title"><h1>{{ __('ui.new_release') }}</h1></div><form class="panel form" method="post" enctype="multipart/form-data" action="{{ route('releases.store') }}">@csrf<label>Product<select name="product_id" required><option value="">{{ __('ui.select') }}</option>@foreach($products as $p)<option value="{{ $p->id }}">{{ $p->name }}</option>@endforeach</select></label><label>{{ __('ui.version') }}<input name="version" required placeholder="3.2.4"></label><label>Channel<select name="channel"><option>stable</option><option>beta</option><option>alpha</option><option>internal</option></select></label><label>{{ __('ui.package') }}<input type="file" name="package" accept=".zip,application/zip" required></label><label>{{ __('ui.release_notes') }}<textarea name="release_notes"></textarea></label><button class="primary">{{ __('ui.save') }}</button></form>@endsection
