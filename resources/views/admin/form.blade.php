@extends('layouts.app') @section('content')<div class="page-title"><h1>{{ $title }}</h1></div><form class="panel form" method="post" action="{{ $action }}">@csrf @foreach($fields as $name=>$type)<label><span>{{ str_replace('_',' ',$name) }}</span>@if($type==='textarea')<textarea name="{{ $name }}">{{ old($name) }}</textarea>@elseif(str_starts_with($type,'select:'))<select name="{{ $name }}">@foreach(explode(',',substr($type,7)) as $option)<option value="{{ $option }}">{{ $option }}</option>@endforeach</select>@else<input type="{{ $type }}" name="{{ $name }}" value="{{ old($name) }}" {{ $name==='name'?'required':'' }}>@endif</label>@endforeach<button class="primary">{{ __('ui.save') }}</button></form>@endsection
