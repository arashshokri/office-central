<!doctype html><html lang="{{ app()->getLocale() }}" dir="{{ app()->getLocale()==='fa'?'rtl':'ltr' }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>{{ __('ui.app') }}</title><link rel="stylesheet" href="{{ asset('assets/app.css') }}"></head><body><div class="shell"><aside><div class="brand"><span>OC</span><strong>{{ __('ui.app') }}</strong></div><nav>@foreach(['dashboard'=>'dashboard','customers.index'=>'customers','products.index'=>'products','releases.index'=>'releases','licenses.index'=>'licenses','installations.index'=>'installations','security.index'=>'security_events','audit.index'=>'audit_logs'] as $route=>$label)<a class="{{ request()->routeIs($route)?'active':'' }}" href="{{ route($route) }}">{{ __('ui.'.$label) }}</a>@endforeach</nav><div class="aside-foot">v{{ config('office.version') }}</div></aside><main><header><button class="menu" onclick="document.body.classList.toggle('open')">☰</button><div class="spacer"></div><form method="post" action="{{ route('locale',app()->getLocale()==='fa'?'en':'fa') }}">@csrf<button class="ghost">{{ __('ui.language') }}</button></form><form method="post" action="{{ route('logout') }}">@csrf<button class="ghost">{{ __('ui.logout') }}</button></form></header><section class="content">@if(session('success'))<div class="flash success">{{ session('success') }}</div>@endif @if(session('license_key'))<div class="license-key"><small>{{ __('ui.full_license_key') }}</small><code>{{ session('license_key') }}</code></div>@endif @if($errors->any())<div class="flash error">{{ $errors->first() }}</div>@endif @yield('content')</section></main></div></body></html>
